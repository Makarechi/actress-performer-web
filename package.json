{
  "name": "actress-performer-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "wasm": "PATH=\"$HOME/.cargo/bin:$PATH\" wasm-pack build wasm-frontend --target web --out-dir ../public/wasm --out-name actress_frontend",
    "predev": "npm run wasm",
    "dev": "astro dev",
    "prebuild": "npm run wasm",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^6.3.3",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "wasm-pack": "^0.15.0"
  }
}
